(
(import aggregate_module '"/lib/yql/aggregate.yql")
(import window_module '"/lib/yql/window.yql")
(import core_module '"/lib/yql/core.yql")
(let world (block '(
  (let x (Read! world (DataSource '"yt" '"plato") (MrTableConcat (Key '('table (String '"Input")))) (Void) '()))
  (let table0 (Right! x))
  (let output (block '(
    (let select (block '(
      (let composite table0)
      (return (UnionAll (block '(
        (let core composite)
        (let core (FlatMap core (lambda '(row) (AsList (AddMember (ForceRemoveMember row 'k) 'k (SafeCast (Member row '"key") (DataType 'Uint32)))))))
        (let preaggregated core)
        (let core (AssumeColumnOrderPartial (Aggregate core '('"k" '"subkey") '('('Count0 (AggApply 'count (ListItemType (TypeOf core)) (lambda '(row) (PersistableRepr (Int32 '"1")))))) '()) '('"k" '"subkey")))
        (let core (PersistableRepr (block '(
          (let projectCoreType (TypeOf core))
          (let addCompositTerms (lambda '(row) (block '(
            (return row)
          ))))
          (let core (SqlProject core '((SqlProjectItem projectCoreType '"column0" (lambda '(row) (block '(
            (let row (Apply addCompositTerms row))
            (let res (Member row 'Count0))
            (return res)
          )))) (SqlProjectItem projectCoreType '"k" (lambda '(row) (block '(
            (let row (Apply addCompositTerms row))
            (let res (Member row '"k"))
            (return res)
          )))) (SqlProjectItem projectCoreType '"subkey" (lambda '(row) (block '(
            (let row (Apply addCompositTerms row))
            (let res (Member row '"subkey"))
            (return res)
          )))))))
          (return core)
        ))))
        (return core)
      )) (block '(
        (let core composite)
        (let core (FlatMap core (lambda '(row) (AsList (AddMember (ForceRemoveMember row 'k) 'k (SafeCast (Member row '"key") (DataType 'Uint32)))))))
        (let preaggregated core)
        (let core (AssumeColumnOrderPartial (Aggregate core '('"k") '('('Count0 (AggApply 'count (ListItemType (TypeOf core)) (lambda '(row) (PersistableRepr (Int32 '"1")))))) '()) '('"k")))
        (let core (PersistableRepr (block '(
          (let projectCoreType (TypeOf core))
          (let addCompositTerms (lambda '(row) (block '(
            (let row (AddMember row '"subkey" (Nothing (MatchType (StructMemberType (ListItemType (TypeOf preaggregated)) 'subkey) 'Optional (lambda '(item) item) (lambda '(item) (OptionalType item))))))
            (return row)
          ))))
          (let core (SqlProject core '((SqlProjectItem projectCoreType '"column0" (lambda '(row) (block '(
            (let row (Apply addCompositTerms row))
            (let res (Member row 'Count0))
            (return res)
          )))) (SqlProjectItem projectCoreType '"k" (lambda '(row) (block '(
            (let row (Apply addCompositTerms row))
            (let res (Member row '"k"))
            (return res)
          )))) (SqlProjectItem projectCoreType '"subkey" (lambda '(row) (block '(
            (let row (Apply addCompositTerms row))
            (let res (Member row '"subkey"))
            (return res)
          )))))))
          (return core)
        ))))
        (return core)
      )) (block '(
        (let core composite)
        (let core (FlatMap core (lambda '(row) (AsList (AddMember (ForceRemoveMember row 'k) 'k (SafeCast (Member row '"key") (DataType 'Uint32)))))))
        (let preaggregated core)
        (let core (AssumeColumnOrderPartial (Aggregate core '() '('('Count0 (AggApply 'count (ListItemType (TypeOf core)) (lambda '(row) (PersistableRepr (Int32 '"1")))))) '()) '()))
        (let core (PersistableRepr (block '(
          (let projectCoreType (TypeOf core))
          (let addCompositTerms (lambda '(row) (block '(
            (let row (AddMember row '"k" (Nothing (MatchType (StructMemberType (ListItemType (TypeOf preaggregated)) 'k) 'Optional (lambda '(item) item) (lambda '(item) (OptionalType item))))))
            (let row (AddMember row '"subkey" (Nothing (MatchType (StructMemberType (ListItemType (TypeOf preaggregated)) 'subkey) 'Optional (lambda '(item) item) (lambda '(item) (OptionalType item))))))
            (return row)
          ))))
          (let core (SqlProject core '((SqlProjectItem projectCoreType '"column0" (lambda '(row) (block '(
            (let row (Apply addCompositTerms row))
            (let res (Member row 'Count0))
            (return res)
          )))) (SqlProjectItem projectCoreType '"k" (lambda '(row) (block '(
            (let row (Apply addCompositTerms row))
            (let res (Member row '"k"))
            (return res)
          )))) (SqlProjectItem projectCoreType '"subkey" (lambda '(row) (block '(
            (let row (Apply addCompositTerms row))
            (let res (Member row '"subkey"))
            (return res)
          )))))))
          (return core)
        ))))
        (return core)
      ))))
    )))
    (let select (Sort select '((Bool 'true) (Bool 'true)) (lambda '(row) '((PersistableRepr (Member row '"k")) (PersistableRepr (Member row '"subkey"))))))
    (return select)
  )))
  (let world (block '(
    (let result_sink (DataSink 'result))
    (let world (Write! world result_sink (Key) output '('('type) '('autoref) '('columns '('"column0" '"k" '"subkey")))))
    (return (Commit! world result_sink))
  )))
  (return world)
)))
(let world (block '(
  (let world (CommitAll! world))
  (return world)
)))
(return world)
)
