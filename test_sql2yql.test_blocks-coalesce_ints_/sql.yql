(
(import aggregate_module '"/lib/yql/aggregate.yql")
(import window_module '"/lib/yql/window.yql")
(import core_module '"/lib/yql/core.yql")
(let world (Configure! world (DataSource '"config") '"UseBlocks"))
(let world (block '(
  (let x (Read! world (DataSource '"yt" '"plato") (MrTableConcat (Key '('table (String '"Input")))) (Void) '()))
  (let table0 (Right! x))
  (let output (block '(
    (let select (block '(
      (let core table0)
      (let core (PersistableRepr (block '(
        (let projectCoreType (TypeOf core))
        (let core (OrderedSqlProject core '((SqlProjectItem projectCoreType '"k1" (lambda '(row) (block '(
          (let res (Member row '"k1"))
          (return res)
        )))) (SqlProjectItem projectCoreType '"column1" (lambda '(row) (block '(
          (let res (Coalesce (Member row '"k2") (Member row '"k1")))
          (return res)
        )))) (SqlProjectItem projectCoreType '"column2" (lambda '(row) (block '(
          (let res (Coalesce (Member row '"k2") (Int32 '"0")))
          (return res)
        )))) (SqlProjectItem projectCoreType '"column3" (lambda '(row) (block '(
          (let res (Coalesce (Member row '"k2") ("/" (Int32 '"1") (Int32 '"0"))))
          (return res)
        )))) (SqlProjectItem projectCoreType '"column4" (lambda '(row) (block '(
          (let res (Coalesce ("/" (Int32 '"1") (Int32 '"2")) (Member row '"k2")))
          (return res)
        )))) (SqlProjectItem projectCoreType '"column5" (lambda '(row) (block '(
          (let res (Coalesce ("/" (Int32 '"1") (Int32 '"2")) (Member row '"k1")))
          (return res)
        )))) (SqlProjectItem projectCoreType '"column6" (lambda '(row) (block '(
          (let res (Coalesce ("/" (Member row '"k1") (Int32 '"0")) (Int32 '"1")))
          (return res)
        )))) (SqlProjectItem projectCoreType '"column7" (lambda '(row) (block '(
          (let res (Coalesce ("/" (Member row '"k1") (Int32 '"0")) (Member row '"k1")))
          (return res)
        )))) (SqlProjectItem projectCoreType '"column8" (lambda '(row) (block '(
          (let res (Coalesce ("/" (Member row '"k1") (Int32 '"2")) (Int32 '"0")))
          (return res)
        )))))))
        (return core)
      ))))
      (return core)
    )))
    (let select (Sort select (Bool 'true) (lambda '(row) (PersistableRepr (Member row '"k1")))))
    (return select)
  )))
  (let world (block '(
    (let result_sink (DataSink 'result))
    (let world (Write! world result_sink (Key) output '('('type) '('autoref) '('columns '('"k1" '"column1" '"column2" '"column3" '"column4" '"column5" '"column6" '"column7" '"column8")))))
    (return (Commit! world result_sink))
  )))
  (return world)
)))
(let world (block '(
  (let world (CommitAll! world))
  (return world)
)))
(return world)
)
