(
(import aggregate_module '"/lib/yql/aggregate.yql")
(import window_module '"/lib/yql/window.yql")
(import core_module '"/lib/yql/core.yql")
(let world (block '(
  (let output (block '(
    (let select (block '(
      (let core (OrderedMap (ToList (AsList (Uint32 '"1") (Uint32 '"2") (Uint32 '"3"))) (lambda '(row) (SelectMembers row '('"Key" '"Value3")))))
      (let core (PersistableRepr (block '(
        (let projectCoreType (TypeOf core))
        (let core (OrderedSqlProject core '((SqlProjectItem projectCoreType '"Key" (lambda '(row) (block '(
          (let res (Member row '"Key"))
          (return res)
        )))) (SqlProjectItem projectCoreType '"Value3" (lambda '(row) (block '(
          (let res (Member row '"Value3"))
          (return res)
        )))))))
        (return core)
      ))))
      (return core)
    )))
    (return select)
  )))
  (let output (Unordered output))
  (let world (block '(
    (let result_sink (DataSink 'result))
    (let world (Write! world result_sink (Key) output '('('type) '('autoref) '('columns '('"Key" '"Value3")))))
    (return (Commit! world result_sink))
  )))
  (return world)
)))
(let world (block '(
  (let world (CommitAll! world))
  (return world)
)))
(return world)
)
